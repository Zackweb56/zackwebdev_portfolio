/**
 * @deprecated
 *
 * GSAP configuration has moved to:
 *   @/frontend/animations/gsap.ts
 *
 * This file is kept temporarily to avoid breaking existing imports.
 * Migrate any imports to:
 *   import { gsap, ScrollTrigger } from "@/frontend/animations/gsap";
 */

export { gsap, ScrollTrigger } from "@/frontend/animations/gsap";
