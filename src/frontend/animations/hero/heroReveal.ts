/**
 * Hero reveal GSAP timeline.
 * Phases: name reveal → subtitle → metadata → decorative elements → cursor active.
 * Uses clip-path + y-transform + opacity.
 * Implemented in Task 4.3 (Build Hero reveal timeline).
 */
export function buildHeroReveal(): void {
  // TODO: Task 4.3
}
