/**
 * Cursor flashlight / radial reveal effect.
 * Uses gsap.quickTo() for smooth inertia following.
 * Implemented in Task 2.5 (Build flashlight effect).
 * Disabled on mobile and when prefers-reduced-motion is set.
 */
export function initFlashlight(): void {
  // TODO: Task 2.5
}

export function destroyFlashlight(): void {
  // TODO: Task 2.5
}
